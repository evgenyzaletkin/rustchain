[package]
name = "rustchain"
version = "0.1.0"
edition = "2024"

[dependencies]
# Use glommio
#tokio = { version = "1.45.1", features = ["rt", "rt-multi-thread", "macros"] }
derive_more = { version = "2.0.1", features = ["display"] }